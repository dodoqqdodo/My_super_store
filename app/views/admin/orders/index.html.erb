<h1> 訂單列表 </h1>

      <table class="table">
          <thead>
              <tr>
                <td>訂單編號</td>
                <td>訂單處理狀態</td>
                <td>處理</td>
              </tr>
          </thead>
          <tbody>
              <% @orders.each do |order| %>
              <tr>
                <td> <%= order.order_id %>  </td>
                <td> <%= order.status %> </td>
                <td> </td>
              </tr>
              <% end %>
          </tbody>
      </table>